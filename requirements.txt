# Core framework and dependencies
Flask
Werkzeug

# Image processing
Pillow

# HTTP requests
requests

# Production server
gunicorn

# Environment variables
python-dotenv

# Security
Flask-Talisman
Flask-SeaSurf

# Rate limiting
Flask-Limiter

# Caching (optional, but useful for performance)
Flask-Caching
redis

# Error tracking (optional, for remote error logging)
sentry-sdk[flask]
